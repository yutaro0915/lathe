# dependencies (match dirs AND pnpm workspace symlinks named node_modules)
node_modules

# next.js
.next/
storybook-static/
out/
dist/
**/dist/
next-env.d.ts
*.tsbuildinfo

# generated local database artifacts (regenerate with `pnpm ingest`)
**/data/*.db
**/data/*.db-*

# playwright
test-results/
playwright-report/
playwright/.cache/

# coverage / test artifacts
coverage/
tmp/

# editor & agent-tool settings (these are personal; don't ship them in a public repo)
.claude/
.cursor/
.codex/
.vscode/
.idea/
.zed/
.agents/
.mcp.json
skills-lock.json

# inner-loop local run state (manifests / logs / escalations / quarantined patches)
.lathe/

# os / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# secrets / env (be aggressive — public repo)
.env
.env.*
!.env.example
*.pem
*.key

# 生会話ログ（資格情報を含むためローカル専用・git 管理外）
_cutover-evidence/conversation-log-raw.md
