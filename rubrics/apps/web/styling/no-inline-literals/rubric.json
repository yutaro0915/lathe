{
  "schema_version": "2",
  "id": "apps/web/styling/no-inline-literals",
  "title": "feature/app の inline style 生 px・生 hex を増やさない — DS gate の blind spot を塞ぐ",
  "version": "1",
  "scope": ["apps/web/components", "apps/web/app"],
  "origin": "監査(2026-06-24)で判明した盲点: spacing-from-token / token-consistency は DS css(apps/web/design-system, app/globals)のみ走査し、feature/app tsx の inline style(`style={{ padding:'12px', color:'#fff' }}`)に書かれた生 px・生 hex は素通りしていた(実測 px 20・hex 6)。token を経由しない値ドリフトを gate 外で許していたため、両者を ratchet 化し『増やさない＋置換で 0 へ駆動』を機械強制する。理想は inline style 禁止＋className=DS class 強制だが、まず literal を ratchet。",
  "checks": [
    {
      "id": "no-new-inline-px",
      "value": "① 何を縛るか: apps/web/components + apps/web/app の tsx で、inline style 中の生 px（`style={{ … 12px … }}`）を増やさない。新規 spacing は DS の 4px グリッド token を className 経由で当てる。② なぜ重要か: spacing-from-token は DS css のみ走査＝feature の inline 生 px は gate 外。放置すると 4px グリッド外の値が gate を迂回して増殖する。③ 仕組み: inline style 中の `数字+px` を grep して数え、現状 20 を baseline にした ratchet。増加で即 RED、token/className へ置換して baseline を下げる。",
      "severity": "major",
      "verify": {
        "kind": "cmd",
        "metric": "count",
        "cmd": "find apps/web/components apps/web/app -name '*.tsx' ! -path '*design-system*' -print0 2>/dev/null | xargs -0 grep -hoE 'style=\\{\\{[^}]*[0-9]+px' 2>/dev/null | wc -l | tr -d ' '",
        "expect": "le:0",
        "tolerance": "ratchet baseline 0（2026-06-25 達成、20→0）。1 つでも増えたら RED。置換で baseline を 0 へ駆動（減らしたら本 expect を下げて締める）。限界: grep ベース。`style={{ width: pct+'%' }}` のような非リテラル動的値は px リテラルが無ければ対象外。className=DS class への移行が本筋で、本 gate はその過程の増殖を止める。"
      },
      "evidence": "inline style 中の生 px の数。0 以下で GREEN、1+ で RED。",
      "examples": {
        "pass": ["spacing は className で DS class（`var(--sp-*)` 使用）を当てる", "既存の inline px を token/class へ置換し count を下げる"],
        "fail": ["feature tsx に `style={{ padding: '10px' }}` を新規追加 → 21 → RED"]
      }
    },
    {
      "id": "no-new-raw-hex",
      "value": "① 何を縛るか: apps/web/components + apps/web/app の tsx で、bare な生 hex 色（`#rrggbb`）を増やさない。色は token（`var(--…)`）経由。② なぜ重要か: token-consistency は DS css/globals のみ＝feature tsx の hex は gate 外＝色ドリフトの盲点。③ 仕組み: `var(...)` を除去してから bare hex を数える＝bare hex は禁止だが `var(--token,#fallback)` の安全 fallback は許容（pre-CSS で token 未ロードでも崩れない正当用途）。0。",
      "severity": "major",
      "verify": {
        "kind": "cmd",
        "metric": "count",
        "cmd": "find apps/web/components apps/web/app -name '*.tsx' ! -path '*design-system*' -print0 2>/dev/null | xargs -0 sed 's/var([^)]*)//g' 2>/dev/null | grep -oE '#[0-9a-fA-F]{6}' | wc -l | tr -d ' '",
        "expect": "le:0",
        "tolerance": "bare hex 0（2026-06-25 達成）。`var(--token,#fallback)` は許容（token 未ロードでも崩れない安全 fallback。app/loading.tsx の 6 件が該当＝正当）。bare hex を 1 つでも足すと RED。限界: var() 除去は単純置換（ネスト var は近似）、6 桁 hex のみ（3 桁 #fff は別途）。色の正本は tokens.css。"
      },
      "evidence": "var() を除いた bare hex の数。0 で GREEN、1+ で RED（var-fallback は除外）。",
      "examples": {
        "pass": ["色は `var(--accent)` を使う", "loading skeleton で `var(--border,#e4e7ec)` の安全 fallback（bare でない）"],
        "fail": ["feature tsx に bare hex `color: '#3b6fd4'` を新規追加 → RED（`var(--accent,#3b6fd4)` なら可）"]
      }
    }
  ]
}
